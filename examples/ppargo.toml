[package]
name = "my_cpp_project"
version = "0.1.0"
edition = "cpp23"

[toolchain]
compiler = "clang++"
linker = "lld"

[dependencies]
# zlib = "1.3.1"
# fmt = { version = "10", features = ["header-only"] }
# mylib = { path = "../mylib" }
# remote = { git = "https://github.com/org/repo.git", rev = "abc123" }

[dev-dependencies]
# catch2 = "3"

[build-dependencies]

[registries]

[features]
package_manager = "vcpkg"
# packages = false         # optional explicit override
vcpkg_root = "C:\\vcpkg"
